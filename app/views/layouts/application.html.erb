<!DOCTYPE html>
<html>
  <head>
    <title>Absolute Identifiers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>

  </head>

  <body>
    <div class="lux">
      <lux-library-header app-name="Absolute Identifier Management" abbr-name="LTR" app-url="<%= root_path %>" theme="dark">
      <% if current_user.blank? %>
        <lux-menu-bar type="main-menu" :menu-items="[
                        {name: 'Create Marc Batch', component: 'MarcBatches', href: '/marc_batches/new'},
                        {name: 'Login', component: 'Login', href: '/users/auth/cas'}
                        ]"
                  ></lux-menu-bar>
      <% else %>
        <lux-menu-bar type="main-menu" :menu-items="[
                        {name: 'Create Marc Batch', component: 'MarcBatches', href: '/marc_batches/new'},
                        {name: 'Logout', component: 'Logout', href: '/sign_out'}
                        ]"
                  ></lux-menu-bar>
      <% end %>
      </lux-library-header>
    </div>
    <div id="main" class="container">
      <div class="row">
        <div class="column">
          <%= render partial: "shared/flash_message" %>
          <%= yield %>
        </div>
      </div>
    </div>
    <div class="lux">
      <lux-library-footer></lux-library-footer>
    </div>
    <div class="deployment-version copyright" >
      Version <span title="<%= GIT_SHA %>"><%= BRANCH %> last updated <%= LAST_DEPLOYED %>.</span>
    </div>
  </body>
</html>
